# Пакет LL(k)-анализа КС-грамматик

## Реализованные алгоритмы

| Алгоритм | Слайды | Описание |
|----------|--------|----------|
| Алг. 2.5 | стр. 175–178 | FIRST_k(A) для всех A ∈ VN (метод неподвижной точки) |
| Алг. 2.6 | стр. 196–200 | σ'(A,B) и σ(A) — контексты нетерминала (Определение 2.12) |
| Алг. 2.7 | стр. 226–230 | FOLLOW_k(A) через σ'(A,B) |
| Алг. 2.4 | стр. 172–173 | Тест LL(k) (Теорема 2.1) |
| —        | Определение 2.7 | Тест сильной LL(k) |

## Структура проекта

```
llk_analyzer/
├── grammar.hpp        — типы: Grammar, Word, Language, k-операции (Лемма 2.1)
├── grammar.cpp        — реализация: парсер грамматики, k-конкатенация
├── algorithms.hpp     — объявления алгоритмов 2.4–2.7
├── algorithms.cpp     — реализация алгоритмов
├── main.cpp           — интерактивный + пакетный режим
├── Makefile           — сборка проекта
├── README.md          — эта инструкция
└── examples/
    ├── ex_2_2_ll1.txt              — Пример 2.2: простая LL(1)
    ├── ex_2_3_nonstrong_ll2.txt    — Пример 2.3: несильная LL(2)
    ├── ex_2_6_expressions.txt      — Пример 2.6: арифм. выражения LL(1)
    ├── ex_2_11_sigma.txt           — Пример 2.11: вычисление σ(A)
    └── ex_left_recursive.txt       — Леворекурсивная (не-LL)
```

## Требования

- Компилятор C++17: `g++ 7+` или `clang++ 5+`
- Linux / macOS / Windows (MinGW/WSL)

## Сборка

```bash
make
```

Исполняемый файл: `./llk_analyzer`

## Запуск

### 1. Запуск из файла грамматики

```bash
./llk_analyzer examples/ex_2_2_ll1.txt
./llk_analyzer examples/ex_2_3_nonstrong_ll2.txt
./llk_analyzer examples/ex_2_6_expressions.txt
./llk_analyzer examples/ex_2_11_sigma.txt
./llk_analyzer examples/ex_left_recursive.txt
```

### 2. Пакетный режим через stdin

```bash
./llk_analyzer < examples/ex_2_2_ll1.txt
```

### 3. Встроенные демо-примеры (все примеры из лекций)

```bash
./llk_analyzer --demo
```

### 4. Интерактивный режим

```bash
./llk_analyzer
```

Команды интерактивного режима:

```
LL(k)> grammar        # ввести грамматику вручную
LL(k)> load examples/ex_2_2_ll1.txt   # загрузить из файла
LL(k)> k 2            # установить lookahead k=2
LL(k)> run            # запустить все алгоритмы
LL(k)> first          # только FIRST_k
LL(k)> follow         # только FOLLOW_k
LL(k)> sigma          # только σ'(A,B) и σ(A)
LL(k)> test           # тест LL(k)
LL(k)> strong         # тест сильной LL(k)
LL(k)> demo           # все примеры из лекций
LL(k)> help           # справка
LL(k)> quit
```

## Формат файла грамматики

```
# Комментарий (игнорируется)
k=2                           # необязательная директива (по умолчанию k=1)
S -> a A a a | b A b a        # правила (альтернативы через |)
A -> b | eps                  # eps = ε-правило
```

Нетерминалы определяются автоматически: это все символы, стоящие в левой
части хотя бы одного правила. Все остальные символы — терминалы.

## Примеры результатов

### Пример 2.2: простая LL(1)-грамматика
```
FIRST_1(S) = { a, b }          FIRST_1(B) = { a, b }
σ(S) = { {eps}, {a,b} }        σ(B) = { {a,b} }
FOLLOW_1(S) = { eps, a, b }    FOLLOW_1(B) = { a, b }
ИТОГ: LL(1) = ДА   |   Сильная LL(1) = ДА
```

### Пример 2.3: несильная LL(2)-грамматика
```
FIRST_2(S) = { aa, ab, bb }    FIRST_2(A) = { eps, b }
FOLLOW_2(A) = { aa, ba }       (совпадает со слайдом 51)
ИТОГ: LL(2) = ДА   |   Сильная LL(2) = НЕТ
```

### Пример 2.6: грамматика арифметических выражений
```
FIRST_1(E) = FIRST_1(T) = FIRST_1(F) = { (, a }
FIRST_1(E') = { eps, + }       FIRST_1(T') = { eps, * }
ИТОГ: LL(1) = ДА   |   Сильная LL(1) = ДА
```

### Пример 2.11: демонстрация σ(A)
```
σ(A) = { {eps, a, b} }         σ(S) = { {eps} }
FOLLOW_1(A) = { eps, a, b }
ИТОГ: LL(1) = ДА   |   Сильная LL(1) = ДА
```

## Тест сборки и примеров

```bash
make test
```

## Связь с теорией

**Определение 2.12 (σ(A)):**
```
σ(A) = { L ⊆ VT^*k | S =>*_lm wAα, w∈VT*, L = FIRST_k(α) }
```

**Вспомогательная функция σ'(A,B):**
```
σ'(A,B) = { L ⊆ VT^*k | A =>*_lm wBα, w∈VT*, L = FIRST_k(α) }
σ(A) = σ'(S, A)
```

**Теорема 2.1 (критерий LL(k)):**
G — LL(k) ⟺ для всех A→α, A→β ∈ P (α≠β), для всех L∈σ(A):
```
(FIRST_k(α) ⊕_k L) ∩ (FIRST_k(β) ⊕_k L) = ∅
```
